VITE_API_BASE_URL=http://localhost:8080

# Offline demo safety net. Set to true to serve pre-baked, engine-verified
# results (Bilal 40 / Aisha 87) with ZERO network or AI calls — the demo can
# never blank out. Leave false for live analysis.
VITE_DEMO_MODE=false
